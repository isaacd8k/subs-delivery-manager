projects:
  subsdeliverymanager:
    schemaPath: amplify/backend/api/subsdeliverymanager/build/schema.graphql
    includes:
      - ./graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: ./graphql/types.ts
        docsFilePath: ./graphql
extensions:
  amplify:
    version: 3
